---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getEntry, render } from 'astro:content';

const about = await getEntry('pages', 'about');
const { Content } = await render(about);
const timeline = about.data.timeline || [];
---

<BaseLayout title="Samar Ahsan" description={about.data.description}>
  <article class="about-page">
    <div class="about-content">
      <Content />
    </div>

    {timeline.length > 0 && (
      <section class="history">
        <h2>A Brief History</h2>
        <div class="timeline">
          <div class="timeline-line"></div>
          {timeline.map((item, index) => (
            <div class={`timeline-item ${index % 2 === 0 ? 'left' : 'right'}`}>
              <div class="timeline-text">
                <h3>{item.title}</h3>
                <span class="timeline-date-mobile">{item.date.toUpperCase()}</span>
                <p>{item.description}</p>
              </div>
              <div class="timeline-connector"></div>
              <div class="timeline-node">
                <span class="timeline-date">{item.date.toUpperCase()}</span>
                <div class="timeline-dot"></div>
              </div>
            </div>
          ))}
        </div>
      </section>
    )}

  </article>
</BaseLayout>

<style>
  .about-page {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .about-content :global(h1) {
    margin-top: 0;
    font-size: 2.5rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
  }

  .about-content :global(h1 + p) {
    font-size: 1.25rem;
    line-height: 1.6;
    color: var(--color-text);
  }

  .about-content :global(h1 + p a) {
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .about-content :global(p + p) {
    font-size: 1.0625rem;
    line-height: 1.7;
    color: var(--color-text-muted);
  }

  /* History Section */
  .history {
    margin-top: 4rem;
  }

  .history h2 {
    font-size: 1.75rem;
    font-weight: 500;
    margin-bottom: 3rem;
    text-align: center;
  }

  /* Timeline */
  .timeline {
    position: relative;
    max-width: 900px;
    margin: 0 auto;
    padding: 1rem 0;
  }

  /* Vertical center line */
  .timeline-line {
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    width: 1px;
    background: var(--color-border);
    transform: translateX(-50%);
  }

  /* Each timeline item - fixed height for even spacing */
  .timeline-item {
    position: relative;
    height: 120px;
    display: flex;
    align-items: center;
  }

  /* Node container - centered on vertical line for date badge */
  .timeline-node {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    z-index: 2;
  }

  /* Dot on the line - positioned at connector end near blurb */
  .timeline-dot {
    position: absolute;
    top: 50%;
    width: 12px;
    height: 12px;
    background: var(--color-bg);
    border: 2px solid var(--color-text);
    border-radius: 50%;
    transform: translateY(-50%);
  }

  /* Date badge */
  .timeline-date {
    background: var(--color-text);
    color: var(--color-bg);
    padding: 0.25rem 0.625rem;
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    white-space: nowrap;
  }

  /* Horizontal connector line */
  .timeline-connector {
    position: absolute;
    top: 50%;
    height: 1px;
    background: var(--color-border);
    width: calc(50% - 160px);
  }

  /* Text content */
  .timeline-text {
    position: absolute;
    width: calc(50% - 180px);
  }

  .timeline-text h3 {
    font-size: 1.0625rem;
    font-weight: 500;
    margin: 0 0 0.375rem;
  }

  .timeline-text p {
    font-size: 0.875rem;
    line-height: 1.5;
    color: var(--color-text-muted);
    margin: 0;
  }

  /* Mobile-only date (hidden on desktop) */
  .timeline-date-mobile {
    display: none;
  }

  /* LEFT side events: text on left, date on right of dot */
  .timeline-item.left .timeline-text {
    left: 0;
    text-align: right;
  }

  .timeline-item.left .timeline-connector {
    left: calc(50% - 160px);
    right: 50%;
  }

  .timeline-item.left .timeline-node {
    flex-direction: row-reverse;
  }

  .timeline-item.left .timeline-dot {
    right: calc(50% + 154px);
    left: auto;
  }

  /* RIGHT side events: text on right, date on left of dot */
  .timeline-item.right .timeline-text {
    right: 0;
    text-align: left;
  }

  .timeline-item.right .timeline-connector {
    left: 50%;
    width: calc(50% - 160px);
  }

  .timeline-item.right .timeline-node {
    flex-direction: row;
  }

  .timeline-item.right .timeline-dot {
    left: calc(50% + 166px);
    right: auto;
  }

  /* ==============================================
     SCROLL ANIMATION STYLES
     Default states: elements are hidden/offset
     .is-visible class triggers the animated state
     ============================================== */

  /* Spine: starts with no height, draws downward */
  .timeline-line {
    transform: translateX(-50%) scaleY(0);
    transform-origin: top center;
    transition: transform 1.2s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .timeline-line.is-visible {
    transform: translateX(-50%) scaleY(1);
  }

  /* Nodes (dot + date): start smaller and transparent */
  .timeline-node {
    opacity: 0;
    transition: opacity 0.5s ease, transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .timeline-dot {
    transform: translateY(-50%) scale(0.5);
    transition: transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .timeline-node.is-visible {
    opacity: 1;
  }

  .timeline-node.is-visible .timeline-dot {
    transform: translateY(-50%) scale(1);
  }

  /* Cards (text): slide in from their respective sides */
  .timeline-text {
    opacity: 0;
    transition: opacity 0.6s ease, transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Left cards slide in from the left */
  .timeline-item.left .timeline-text {
    transform: translateX(-30px);
  }

  /* Right cards slide in from the right */
  .timeline-item.right .timeline-text {
    transform: translateX(30px);
  }

  .timeline-text.is-visible {
    opacity: 1;
    transform: translateX(0);
  }

  /* Connector lines: fade in */
  .timeline-connector {
    opacity: 0;
    transition: opacity 0.5s ease 0.2s;
  }

  .timeline-connector.is-visible {
    opacity: 1;
  }

  /* Respect reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .timeline-line,
    .timeline-node,
    .timeline-dot,
    .timeline-text,
    .timeline-connector {
      transition: none;
      opacity: 1;
      transform: none;
    }

    .timeline-line {
      transform: translateX(-50%) scaleY(1);
    }
  }

  /* Mobile */
  @media (max-width: 768px) {
    .about-content :global(h1) {
      font-size: 2rem;
    }

    .about-content :global(h1 + p) {
      font-size: 1.125rem;
    }

    .timeline {
      padding: 1rem 0 1rem 0;
    }

    .timeline-line {
      left: 6px;
      transform: none;
    }

    .timeline-item {
      height: auto;
      min-height: auto;
      padding: 0 0 2rem 2rem;
      display: block;
    }

    .timeline-node {
      position: absolute;
      left: 0;
      top: 0;
      transform: none;
      flex-direction: row;
      align-items: center;
      gap: 0;
    }

    .timeline-item.left .timeline-node,
    .timeline-item.right .timeline-node {
      flex-direction: row;
    }

    .timeline-dot {
      position: static;
      transform: none;
      flex-shrink: 0;
    }

    .timeline-item.left .timeline-dot,
    .timeline-item.right .timeline-dot {
      left: auto;
      right: auto;
    }

    .timeline-date {
      display: none;
    }

    .timeline-date-mobile {
      display: block;
      font-size: 0.7rem;
      font-weight: 600;
      letter-spacing: 0.05em;
      color: var(--color-text-muted);
      margin: 0.25rem 0 0.5rem;
    }

    .timeline-connector {
      display: none;
    }

    .timeline-text {
      position: relative;
      width: 100%;
      left: auto;
      right: auto;
      padding-left: 0;
    }

    .timeline-text h3 {
      font-size: 1rem;
      margin-bottom: 0;
    }

    .timeline-text p {
      font-size: 0.8125rem;
      margin-top: 0;
    }

    .timeline-item.left .timeline-text,
    .timeline-item.right .timeline-text {
      text-align: left;
    }

    /* Mobile animation adjustments */
    .timeline-line {
      transform: scaleY(0);
      transform-origin: top center;
    }

    .timeline-line.is-visible {
      transform: scaleY(1);
    }

    /* On mobile, all cards slide from left */
    .timeline-item.left .timeline-text,
    .timeline-item.right .timeline-text {
      transform: translateX(-20px);
    }

    .timeline-text.is-visible {
      transform: translateX(0);
    }
  }
</style>

<script>
  /**
   * Timeline Scroll Animation
   *
   * Uses IntersectionObserver to trigger animations as elements enter the viewport.
   * - Spine draws downward when timeline section becomes visible
   * - Nodes pop in when they enter the viewport
   * - Cards slide in from their respective sides
   *
   * Respects prefers-reduced-motion by checking media query before animating.
   * Graceful degradation: content is visible even if JS fails (via CSS :not(.js-enabled) or noscript).
   */

  // Only run animations if user hasn't requested reduced motion
  const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (!prefersReducedMotion) {
    // Get all animatable elements
    const timelineLine = document.querySelector('.timeline-line');
    const timelineItems = document.querySelectorAll('.timeline-item');

    /**
     * Observer for the spine (timeline-line)
     * Triggers when the timeline section enters the viewport
     * Uses a lower threshold so it starts drawing early
     */
    const spineObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            // Once visible, no need to observe anymore
            spineObserver.unobserve(entry.target);
          }
        });
      },
      {
        threshold: 0,
        rootMargin: '0px 0px -10% 0px' // Trigger slightly before fully in view
      }
    );

    /**
     * Observer for timeline items (nodes, cards, connectors)
     * Each item animates independently when it enters the viewport
     * Uses a higher threshold so items animate when more visible
     */
    const itemObserver = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const item = entry.target;

            // Find child elements to animate
            const node = item.querySelector('.timeline-node');
            const text = item.querySelector('.timeline-text');
            const connector = item.querySelector('.timeline-connector');

            // Stagger the animations slightly
            // Node pops first
            if (node) {
              node.classList.add('is-visible');
            }

            // Card slides in shortly after
            if (text) {
              setTimeout(() => {
                text.classList.add('is-visible');
              }, 100);
            }

            // Connector fades in last
            if (connector) {
              setTimeout(() => {
                connector.classList.add('is-visible');
              }, 150);
            }

            // Once animated, stop observing
            itemObserver.unobserve(item);
          }
        });
      },
      {
        threshold: 0.2, // Trigger when 20% of item is visible
        rootMargin: '0px 0px -50px 0px' // Small offset from bottom
      }
    );

    // Start observing elements
    if (timelineLine) {
      spineObserver.observe(timelineLine);
    }

    timelineItems.forEach((item) => {
      itemObserver.observe(item);
    });
  } else {
    // If reduced motion is preferred, make everything visible immediately
    document.querySelectorAll('.timeline-line, .timeline-node, .timeline-text, .timeline-connector')
      .forEach((el) => el.classList.add('is-visible'));
  }
</script>
